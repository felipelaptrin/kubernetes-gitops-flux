apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: alb-controller
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./k8s/addons/dev/alb-controller
  prune: true
  dependsOn:
    - name: karpenter
  sourceRef:
    kind: GitRepository
    name: flux-system
  postBuild:
    substituteFrom:
      - kind: ConfigMap
        name: cluster-vars-terraform
